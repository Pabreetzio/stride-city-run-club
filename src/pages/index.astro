---
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
import { getFeaturedEvent } from '../lib/events';
import '../styles/home.scss';

const recentPosts = (await getCollection('blog'))
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
	.slice(0, 3);

// The next race to spotlight (featured upcoming event, else soonest upcoming).
const nextRace = await getFeaturedEvent();

// Community partners / sponsors. Logos live in /public/sponsors, extracted from
// the "Sponsors Logo.ai" file Ari sent. Where a partner has a neighborhood
// location near the club, the link points to that specific spot.
const sponsors = [
	{
		name: 'Edge City Brewery',
		logo: '/sponsors/edge-city-brewery.png',
		url: 'https://edgecitybrewery-clt.com/',
	},
	{
		name: 'Move with Ari — Key NC Homes',
		logo: '/sponsors/move-with-ari.png',
		url: 'https://www.keynchomes.com/home',
	},
	{
		name: "Hawthorne's NY Pizza & Bar (Old Post Rd)",
		logo: '/sponsors/hawthornes-pizza.png',
		url: 'https://order.online/store/hawthorne%27s-ny-pizza-and-bar-charlotte-131770/',
	},
	{
		name: 'East Side Animal Hospital',
		logo: '/sponsors/east-side-animal-hospital.png',
		url: 'https://eastsideahclt.com/',
	},
	{
		name: 'Anytime Fitness (Oakhurst)',
		logo: '/sponsors/anytime-fitness.png',
		url: 'https://www.anytimefitness.com/locations/charlotte-north-carolina-4913',
	},
	{
		name: 'Monroe Road Advocates (MoRA)',
		logo: '/sponsors/mora.png',
		url: 'https://moraclt.org/',
	},
	{
		name: 'Smoothie King (Old Post Rd)',
		logo: '/sponsors/smoothie-king.png',
		url: 'https://locations.smoothieking.com/ll/us/nc/charlotte/6215-old-post-road-ste-102/',
	},
];
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />

		<section class="hero">
			<div class="hero-inner">
				<h1><span class="accent">Running</span> Together,<br />Building Community</h1>
				<div class="hero-sub-wrap">
					<p class="hero-sub">
						Join us every Wednesday night at Edge City Brewery at 6:30pm. All ages and fitness levels welcome, walk or run. The route is two miles.
					</p>
				</div>
				{/* <a href="/runs" class="btn-primary">See Upcoming Runs</a> */}
			</div>
		</section>

		{nextRace && (
			<section class="next-race">
				<div class="next-race-inner">
					{nextRace.data.image && (
						<a href={`/events/${nextRace.id}`} class="next-race-media">
							<Image
								src={nextRace.data.image}
								alt={nextRace.data.imageAlt ?? nextRace.data.title}
								width={760}
							/>
						</a>
					)}
					<div class="next-race-content">
						<p class="next-race-kicker">Our Next Team Race</p>
						<h2>{nextRace.data.title}</h2>
						{nextRace.data.dateLabel && (
							<p class="next-race-date">{nextRace.data.dateLabel}</p>
						)}
						{nextRace.data.location && (
							<p class="next-race-location">{nextRace.data.location}</p>
						)}
						<p class="next-race-desc">{nextRace.data.description}</p>
						<div class="next-race-actions">
							<a href={`/events/${nextRace.id}`} class="btn-primary">Race details</a>
							{nextRace.data.registerUrl && (
								<a
									href={nextRace.data.registerUrl}
									class="btn-secondary"
									target="_blank"
									rel="noopener"
								>
									Register
								</a>
							)}
						</div>
					</div>
				</div>
			</section>
		)}

		<section class="about-strip">
			<div class="container">
				<h2>Overview</h2>
				<p>
					Whether you’re lacing up for your first mile or training for a personal best, Stride City Run Club
					is built around one simple truth: movement connects people. Our weekly runs offer more than
					workouts; they're chances to meet neighbors, create consistency, and celebrate progress.
					Joining is easy. Show up to a Wednesday or Sunday meet, sign in, and pick your pace group.
					We pair new members with experienced volunteers who know the routes. If you prefer to plan
					ahead, register via RunSignUp or our Google Form so we can welcome you and keep you
					informed about any route changes, weather updates, or special events. We believe in running
					with purpose. Our Safety First protocol means every run follows key rules: wear visible gear at
					night, run single-file on narrow sidewalks, and signal clearly when passing.
				</p>
				<p>
					We also work with local partners and the city to advocate for safer, more walkable streets,
					including wider sidewalks, shared-use paths, and public art projects that inspire. Community
					impact is central to us. Since 2021, we’ve welcomed over 2,000 participants and logged more
					than 3,000 miles. These numbers reflect real people changing habits and supporting one
					another.
				</p>
				<p>
					Edge City Brewery, Embrace Art, and OMG Donuts host post-run hangouts, provide water, and
					offer fun post-run rewards that strengthen neighborhood ties.
				</p>
				<p>
					Volunteers keep our club running. Pacers, route marshals, event planners, and social media
					helpers all have meaningful ways to share their skills. Volunteering grows the club’s reach and
					deepens your connection to the community. Looking ahead, we have big dreams: family-friendly
					5Ks and 10Ks, marathon options for seasoned runners, and inclusive events that spotlight
					Paralympic athletes. We're also developing youth initiatives and accessible programming, so
					kids, seniors, and people with disabilities can join in the joy of movement. For new members:
					start with the 2-mile loop, gradually increase distance, and invest in good shoes. Bring water,
					wear reflective gear for evening runs, and tell a volunteer about any health concerns before
					starting.
				</p>
				<p>
					Most importantly, celebrate small wins, a faster mile, a consistent week, or simply showing up.
					Want to be featured? Email us your story or tag us on social, and you could appear in our next
					newsletter. If you're interested in joining a group that values safety, inclusivity, and local impact,
					sign up through our registration page. Explore schedules, volunteering, and upcoming races
					there. Take your next step toward a healthier, more connected life with us.
				</p>
			</div>
		</section>

		<section class="sponsors">
			<div class="container">
				<h2>Our Community Partners</h2>
				<p class="sponsors-intro">
					Stride City runs on the support of local businesses and neighbors along Monroe Road.
					Show them some love the next time you're in the neighborhood.
				</p>
				<ul class="sponsor-grid">
					{sponsors.map((sponsor) => (
						<li class="sponsor-card">
							<a
								href={sponsor.url}
								target="_blank"
								rel="noopener sponsored"
								aria-label={sponsor.name}
							>
								<img src={sponsor.logo} alt={`${sponsor.name} logo`} loading="lazy" />
							</a>
						</li>
					))}
				</ul>
			</div>
		</section>

		{/* Latest News section — uncomment when blog/news is ready
		{recentPosts.length > 0 && (
			<section class="news">
				<div class="container">
					<h2>Latest News</h2>
					<ul class="post-list">
						{recentPosts.map((post) => (
							<li>
								<a href={`/blog/${post.id}/`}>
									<span class="post-title">{post.data.title}</span>
									<span class="post-date"><FormattedDate date={post.data.pubDate} /></span>
								</a>
							</li>
						))}
					</ul>
					<a href="/blog" class="btn-secondary">All Posts</a>
				</div>
			</section>
		)}
		*/}

		<Footer />
	</body>
</html>
